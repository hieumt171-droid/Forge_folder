import _toConsumableArray from "@babel/runtime/helpers/toConsumableArray";
import { optimizeLabelStackWithRegistry } from '../common/utils';
export function optimizeSpans(spans, interactionStart, reactUFOVersion, registry) {
  var updatedSpans = spans.reduce(function (result, span) {
    var labelStack = span.labelStack,
      type = span.type,
      name = span.name,
      start = span.start,
      end = span.end;
    if (labelStack && start >= interactionStart) {
      result.push({
        labelStack: optimizeLabelStackWithRegistry([].concat(_toConsumableArray(labelStack), [{
          name: name
        }]), reactUFOVersion, registry),
        startTime: Math.round(start),
        endTime: Math.round(end),
        type: type
      });
    }
    return result;
  }, []);
  return updatedSpans;
}