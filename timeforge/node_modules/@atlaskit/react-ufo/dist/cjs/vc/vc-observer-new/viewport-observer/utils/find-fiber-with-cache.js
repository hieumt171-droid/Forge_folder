"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = findFiberWithCache;
var _findReactFiber = _interopRequireDefault(require("./find-react-fiber"));
function findFiberWithCache(element, maxLevel, checkedNodes, resultCache) {
  var currentElement = element;
  var remainingLevels = maxLevel;
  while (currentElement && remainingLevels > 0) {
    var cachedResult = resultCache === null || resultCache === void 0 ? void 0 : resultCache.get(currentElement);
    if (cachedResult !== undefined) {
      return {
        fiber: null,
        cachedResult: cachedResult
      };
    }
    checkedNodes.push(currentElement);
    var fiber = (0, _findReactFiber.default)(currentElement);
    if (fiber) {
      return {
        fiber: fiber
      };
    }
    currentElement = currentElement.parentElement;
    remainingLevels--;
  }
  return {
    fiber: null
  };
}