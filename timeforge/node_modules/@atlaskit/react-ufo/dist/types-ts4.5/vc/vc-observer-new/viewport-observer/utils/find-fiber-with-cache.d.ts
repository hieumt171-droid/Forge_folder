type FindFiberResult = {
    fiber: any | null;
    cachedResult?: boolean;
};
export default function findFiberWithCache(element: HTMLElement, maxLevel: number, checkedNodes: HTMLElement[], resultCache?: WeakMap<HTMLElement, boolean>): FindFiberResult;
export {};
