import type { InteractionMetrics } from '../../common';
import type { OptimizedLabelStack } from '../common/types';
import type { LabelStackRegistry } from '../common/utils/label-stack-registry';
import type { getReactUFOPayloadVersion } from './get-react-ufo-payload-version';
export declare function optimizeSpans(spans: InteractionMetrics['spans'], interactionStart: number, reactUFOVersion: ReturnType<typeof getReactUFOPayloadVersion>, registry?: LabelStackRegistry): {
    labelStack: OptimizedLabelStack;
    startTime: number;
    endTime: number;
    type: string;
}[];
