import { traverse } from '../traverse/traverse';
import { validator } from '../validator/validator';
var NATIVE_EMBED_EXTENSION_KEY = 'native-embed';

/**
 * Returns the set of parent node names whose content expression in the
 * supplied schema allows an `embedCard` child. Returns an empty set if the
 * schema does not declare an `embedCard` node.
 */
var getEmbedCardAllowedParentTypes = function getEmbedCardAllowedParentTypes(schema) {
  var embedCardType = schema.nodes.embedCard;
  if (!embedCardType) {
    return new Set();
  }
  var allowed = new Set();
  for (var _i = 0, _Object$values = Object.values(schema.nodes); _i < _Object$values.length; _i++) {
    var _nodeType$contentMatc;
    var nodeType = _Object$values[_i];
    if ((_nodeType$contentMatc = nodeType.contentMatch) !== null && _nodeType$contentMatc !== void 0 && _nodeType$contentMatc.matchType(embedCardType)) {
      allowed.add(nodeType.name);
    }
  }
  return allowed;
};

/**
 * Replaces `extension` nodes whose `extensionKey` starts with `native-embed`
 * with an `embedCard` when the parent context allows it (per the supplied
 * schema), otherwise a `paragraph` containing an `inlineCard`. Native-embed
 * nodes with no URL are dropped. If the transformed ADF fails schema
 * validation the original ADF is returned.
 */
export var nativeEmbedsFallbackTransform = function nativeEmbedsFallbackTransform(adf, schema) {
  var embedCardAllowedParentTypes = getEmbedCardAllowedParentTypes(schema);
  var isEmbedCardAllowedInParent = function isEmbedCardAllowedInParent(parent) {
    var _parent$node;
    var parentType = parent === null || parent === void 0 || (_parent$node = parent.node) === null || _parent$node === void 0 ? void 0 : _parent$node.type;
    return parentType ? embedCardAllowedParentTypes.has(parentType) : false;
  };
  var didTransform = false;
  var transformedAdf = traverse(adf, {
    extension: function extension(node, parent) {
      var _node$attrs, _node$attrs2;
      if (((_node$attrs = node.attrs) === null || _node$attrs === void 0 ? void 0 : _node$attrs['extensionKey'].split(':')[0]) !== NATIVE_EMBED_EXTENSION_KEY) {
        return node;
      }
      var url = (_node$attrs2 = node.attrs) === null || _node$attrs2 === void 0 || (_node$attrs2 = _node$attrs2['parameters']) === null || _node$attrs2 === void 0 || (_node$attrs2 = _node$attrs2.macroParams) === null || _node$attrs2 === void 0 || (_node$attrs2 = _node$attrs2.url) === null || _node$attrs2 === void 0 ? void 0 : _node$attrs2.value;
      if (!url) {
        didTransform = true;
        return false;
      }
      didTransform = true;
      if (isEmbedCardAllowedInParent(parent)) {
        return {
          type: 'embedCard',
          attrs: {
            url: url,
            layout: 'center'
          }
        };
      }
      return {
        type: 'paragraph',
        content: [{
          type: 'inlineCard',
          attrs: {
            url: url
          }
        }]
      };
    }
  });
  if (didTransform && transformedAdf) {
    try {
      var validate = validator();
      var _validate = validate(transformedAdf),
        valid = _validate.valid;
      if (!valid) {
        // Transformed ADF is invalid – fall back to the original document
        return {
          transformedAdf: adf,
          hasValidTransform: false
        };
      }
    } catch (_unused) {
      // Validation threw – fall back to the original document
      return {
        transformedAdf: adf,
        hasValidTransform: false
      };
    }
  }
  return {
    transformedAdf: transformedAdf,
    hasValidTransform: didTransform
  };
};