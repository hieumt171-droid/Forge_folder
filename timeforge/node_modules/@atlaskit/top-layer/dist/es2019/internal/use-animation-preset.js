import { useInsertionEffect } from 'react';
import { ensurePresetStyles } from './ensure-preset-styles';
/**
 * Normalizes the `animate` prop to a preset (or null) and ensures its CSS
 * is injected once into document.head. Used by PopupContent, Popover, and
 * DialogContent so they do not duplicate this logic.
 */
export function useAnimationPreset(animate) {
  const preset = animate != null && animate !== false ? animate : null;
  useInsertionEffect(() => {
    if (preset) {
      ensurePresetStyles({
        preset
      });
    }
  }, [preset]);
  return preset;
}