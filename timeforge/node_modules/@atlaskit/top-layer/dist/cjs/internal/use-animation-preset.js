"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useAnimationPreset = useAnimationPreset;
var _react = require("react");
var _ensurePresetStyles = require("./ensure-preset-styles");
/**
 * Normalizes the `animate` prop to a preset (or null) and ensures its CSS
 * is injected once into document.head. Used by PopupContent, Popover, and
 * DialogContent so they do not duplicate this logic.
 */
function useAnimationPreset(animate) {
  var preset = animate != null && animate !== false ? animate : null;
  (0, _react.useInsertionEffect)(function () {
    if (preset) {
      (0, _ensurePresetStyles.ensurePresetStyles)({
        preset: preset
      });
    }
  }, [preset]);
  return preset;
}