"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.usePresetStyles = usePresetStyles;
var _react = require("react");
var _ensurePresetStyles = require("./ensure-preset-styles");
/**
 * Normalizes a preset prop to a value (or null) and ensures its CSS
 * is injected once into `document.head`.
 *
 * Works with any preset that has `{ css, name }` - animation presets
 * (TAnimationPreset) satisfies this shape.
 */
function usePresetStyles(_ref) {
  var preset = _ref.preset;
  var resolved = preset != null && preset !== false ? preset : null;
  (0, _react.useInsertionEffect)(function () {
    if (resolved) {
      (0, _ensurePresetStyles.ensurePresetStyles)({
        preset: resolved
      });
    }
  }, [resolved]);
  return resolved;
}